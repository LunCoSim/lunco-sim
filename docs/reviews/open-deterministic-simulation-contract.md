# Open — whole-simulation deterministic execution

**Reviewed:** 2026-09-25
**Scope:** USD lifecycle/projection, Modelica co-simulation, SysML analysis,
Rhai execution, Avian physics, and async preparation.
**Verdict:** The runtime has useful fixed-step and causal-barrier foundations,
but it cannot currently guarantee that async completion, untracked script
dependencies, or ECS query order leave the same authoritative state at each
tick. Stable actor/event/worker submission order is implemented; the whole
contract remains open.

## Findings

| ID | Severity | Finding and evidence | Status |
|---|---|---|---|
| D1 | P1 | Scene start composes owner-specific deterministic boundaries rather than one global readiness bit. The root USD loader fetches and composes its available dependency closure before publishing the stage asset; the lifecycle progress key now ends after structural projection, independent of async CPU mesh construction. Active primary references and causal Modelica compilation hold exact `SimulationProgress` keys. Primary-scene reference closures prepare in parallel, while live-stage mutation commits the ready operation prefix in authored order; a later ready asset retains its ready state until earlier active references commit. The USD terrain bridge and progress scan run in `PreUpdate` before `TimeSpineSet`, including a pending Twin manifest scan, so terrain data/collider work holds simulation before its first eligible fixed tick. The terrain key remains through the browser worker's coarse-to-full result. Physics readiness holds the solver clock for fixed-step body and joint admission, and scenario hooks remain closed until readiness clears. Initial-pose validation waits for resolved joint body topology, not later solver-island admission, and applies the runtime joint-pair collision exclusions to its support checks. The production `sensor` fixture captures its first behavior call and checks finite reads from both declared Modelica participants and admitted physics ports; an initial altimeter miss remains explicitly invalid until the first Avian ray sample. Its Rhai test also attaches two scenarios to authored actors with `RunScenario`: the lower `GlobalEntityId` actor writes a temporary marker and the higher actor must observe and restore it during `on_start`. On 2026-09-25, `python3 scripts/compare_deterministic_startup_dependencies.py` ran this production scene four times on the main-integrated build: twice with Compute width 1 and twice with the default width 24. All four runs passed both the Rhai actor-order assertion and `SENSORS`; the exact tick-10 first-behavior trace, including scene generation and actor identity plus IMU, altimeter, and contact values, matched with digest `8ad6116be742467fe9d20f40e6e4b8e9c88ad59d587917550a08a0a3e7fe6bf5`. Scene-readiness holds varied from 768 to 1,370 Update passes; physics-admission holds were 4 passes and participant-readiness holds were 8–9 passes. Each run took 1.1–1.2 seconds. This proves same-build first-observation equality and stable actor start order for one authored dependency graph and these profiles; it does not force opposite worker completion order or establish complete scene closure. | Partial; sensor and actor-order acceptance pass across two profiles, complete scene closure and controlled async completion-order coverage remain open |
| D2 | P1 | Dynamic referenced assets used to become live when asynchronous loads completed: [`drain_ref_spawns`](../../crates/lunco-usd-bevy-runtime-core/src/twin_projection.rs) authored and projected ready references without a simulation admission hold. Active references on the mounted primary scene now carry an exact `SceneReferences` progress key through prepared-plan consumption by live ECS projection. Their assets still prepare concurrently, but the owner commits or publishes terminal failures only for the completed operation prefix in authored order and retains later ready or failed outcomes; preview and additive mounts cannot pause or fault the primary simulation. Failed primary closure/projection keeps the hold and publishes a terminal fault, while cancellation releases only that operation. Initial authored USD composition dependencies are fetched and composed by the root stage asset loader before structural projection. The production `route_lifecycle` Rhai gate passed twice, admitting a new landing-marker reference before a cached route-point reference and checking both projected entities on the first resumed fixed call. A focused owner test forces the later authored result ready first, verifies both references remain unprojected while the prefix is incomplete, then verifies authored commit order when the earlier result becomes ready. | Partial; scene admission and forced owner-boundary completion-order coverage pass, while complete composition closure and asset-server-to-commit race coverage remain open |
| D3 | P1 | Rhai port reads/writes, targeted commands, and Modelica-event consumption can affect the live simulation outside USD's `SimConnection` closure. `simulation_dependencies(me, ctx)` resolves each declared id to a live Modelica participant and stores the scenario's contribution separately from aggregate solver-barrier membership. At simulation time, port access through `get`, `port`, and `ReadPorts`, targeted commands, and event delivery are checked against the calling scenario's own plan; presence in USD wiring or another scenario's plan is not authorization. Required owner inputs hold activation until Ready, with failure/missing-owner diagnostics. Production `sensor` passed with 46 Rhai assertions covering rejection of a non-Modelica dependency, planning-phase access, undeclared port read, `ReadPorts`, `SetPorts` write and Modelica-event consumption, live Modelica worker diagnostics, and full versus compact `CosimStatus` responses. Reflected ECS reads and direct script effects still lack a complete dependency/action record. | Partial; per-scenario Modelica access and owner-input readiness are covered, while general ECS dependencies and the typed action/replay path remain open |
| D4 | P1 | `LunCoSimSimulationPlugin` snapshots the initialized `ComputeTaskPool` width into [`PhysicsComputeProfile`](../../crates/lunco-physics/src/lib.rs); production `scripting_task_contract` verifies the profile path. `build_core_app(None)` uses `TaskPoolOptions::default()`, matching GUI `DefaultPlugins`; the scene-test `--threads 0` selects it, while the normal headless server entry point currently pins one Compute thread. Avian 0.7 uses Compute for parallel broad/narrow-phase work. USD projection assigns generic `PhysicsOrderKey` values from instance roots and authored prim paths. `DeterministicJointSolverPlugin` prepares stable joint order after `SolverSystems::PrepareJoints`; native joint solves, motor warm-start, and custom prismatic corrections all consume that order. Mobility applies raycast suspension/tire forces, jointed tire forces, and raycast mass-property folds in authored key order. Missing, empty, or duplicate keys raise a runtime fault and hold physics. The production `multi_rover_stress_20` Rhai gate compares exact physics and Modelica snapshots. On the main-integrated build, one four-run matrix at Compute widths 1 and 24 matched all six checkpoints, 32 per-tick samples, 240 Modelica participant snapshots, and all 20 articulated-body states in every run, with digest `0a080decafbe69264944d141da6afa3e02a87514e2d1da4e92d2317713f82dd1`. Every run produced the Rhai stress verdict. Startup readiness spanned 3,760–4,645 Update passes; physics-admission holds were 5 passes, participant-readiness holds were 11–12, and scene runs took 26.8–27.1 seconds including startup. This is same-build evidence for the stress fixture and these pool widths; whole-simulation replay inputs, cross-machine floating-point behavior, broader scene coverage, and physics throughput remain open. | Partial; same-build and cross-profile state matches in the joint stress fixture, while whole-session replay and broader coverage remain open |
| D5 | P2 | Inline scenario roots and source-asset AST cache misses use bounded native `AsyncWorkAdmission`; file-backed `RhaiSource` assets are parsed in the Bevy async asset loader, then publish exact text/AST pairs under their canonical ids. The publisher retains each live asset id's canonical path through `Removed`, because Bevy drops its reverse path lookup when the final handle is released; a stale id cannot retire a newer asset for the same canonical source. Scenario workers reuse matching ASTs, and the owner validates the discovered closure and commits module ASTs before activation. The scenario resolver rejects missing preparation instead of compiling on the live lifecycle path. `route_nested_collider` passes through the production binary with the attached `route_follow` scenario importing `waypoint_editor`. Module bodies, top-level initialization, and hooks still execute in stable actor order because they can run world behavior. A browser worker transport remains open. | Partial; native transitive preparation has a production scene gate and asset retirement passes its focused Rust test plus the rebuilt production restart gate; whole-sim acceptance and wasm transport remain open |
| D6 | P2 | Twin `AnalyzeSysml` and `ValidateSysml` read immutable source-set snapshots prepared through `AsyncWorkAdmission`; the owner fences commits by Twin id, root, and operation ([`PrepareTwinSysmlAnalysis`](../../crates/lunco-sysml/src/analysis.rs), [`validate_sysml_twin`](../../crates/lunco-scene-validation/src/validate.rs)). The Twin analysis producer registers `sysml.twin-analysis` and publishes exact-name Pending/Ready/Failed states into generic `SimulationDependencyStates`; a scenario that declares that key holds only its existing activation boundary and resumes on an owner revision. The production `sysml_async_analysis` scene passes through `luncosim test` with this dependency declared and verifies the committed source-set snapshot, source revision, authored requirement usage, positive verification coverage through resolved element handles, exclusion of another same-snapshot requirement, and explicit unmounted-Twin error. Editor/Twin analysis alone never holds `SimulationProgress`. Open `SysmlDocument` source edits are cheap; lifecycle events snapshot source/origin and shared admission prepares parser/resolver facts at `Interactive` priority. `SysmlDocumentAnalyses` fences commits by document id, generation, origin URI, and operation. `InspectSysmlDocument` reports pending/ready/failed states, with null diagnostics until a matching result commits; the editor returns pending as retryable. Authored Rhai scene coverage checks pending UX and parser diagnostics for the exact edited generation. Browser worker transport remains open. | Partial; native scenario dependency acceptance passes, browser worker transport remains |
| D7 | P2 | Default Twin source parsing and persistent-source serialization now use native `AsyncWorkAdmission`. `PreparedFileBacked` keeps registry path identity and dirty-document policy at commit; the owner checks the exact current source text, restores the runtime layer, and accepts serialized output only for its captured document generation. The live USD stage stays with its thread-affine owner. Runtime sidecar read/parse and editor-preview initial serialization remain synchronous, and wasm worker transport is not installed for this path. | Partial; default native parse/serialization is off the main schedule, while runtime restore and browser transport remain open |
| D8 | P2 | `ModelicaSourceLoader` publishes text plus its prepared `ModelInterface` and sorted required-root set as one asset revision. Native document-source parsing uses shared bounded priority admission. Native source-root reads, bound-input extraction, and AST parsing run on the Modelica preparation pool. A dedicated Rumoca actor owns the mutable session and shared DAE cache; source-root installs and ordinary `Compile` requests share a FIFO, and results commit to the worker in submission order with entity-session and library-generation fences. This keeps ordinary compile/install work off the Modelica command owner while preserving source-root-before-dependent-compile order. Immutable DAE lowering plus persistent solve-cache reads, decoding, encoding, and writes run on the bounded solve-preparation pool, with admission bounded across the compile/installation/lowering pipeline. Reset, parameter update, and cache-invalidating Step auto-init now continue through the same asynchronous actor FIFO; owner results commit in submission order after entity-session and library-generation fencing, with Step rebuilds sharing bounded admission. A failed root admission remains attached to the compiler so dependent compiles receive the retained error; root discovery lists bundled filenames without reading the full directory payload. WASM host storage reads remain synchronous and shared-priority browser admission is not wired. First-compile intent stays in `ModelicaSet::AdmitCompileRequests`; the execution plugin consumes it and the worker owns document resolution. Solver stepping remains in `FixedUpdate`; active causal models hold `SimulationProgress` through compile-result commit, and session/generation fences discard stale compile output while preserving current run intent. | Partial; native compile, reset, update, and cache-invalidating Step rebuilds are asynchronous; browser admission and whole-scene admission remain open |
| D9 | P2 | Networking rollback replays recorded `InputFrame`s for an owned body assembly, including the complete ordered, latched `SetPorts` setpoints; it also re-derives co-simulation ports if live `Time<Virtual>` is paused. The controller regression test verifies arbitrary channels and partial-write latching, and `lunco-networking-core` compiles against the frame contract. This bounded body rollback is not a whole-session authoritative command/script input log or replay verdict; USD lifecycle mutations, Rhai state, and Modelica state are not reconstructed by this path ([`command-journal.md`](../architecture/command-journal.md#L1)). Adaptive Modelica also has an explicit non-bitwise-reproducible numerical profile ([`28-modelica-realtime-physics.md`](../architecture/28-modelica-realtime-physics.md#L26)). A precise guarantee must separate tick/order determinism from numeric cross-platform replay. | Partial; complete controlled-body port capture is in place, while whole-session replay and numeric cross-run evidence remain open |
| D10 | Fixed | Scenario hooks and teardown previously followed ECS/hash iteration order even though synchronous `cmd()` can affect a later hook in the same pass. The driver reads the source-owned `GlobalEntityId` component directly for compile submission, completion commit, and hook order; it does not mistake a not-yet-synchronized API registry entry for a local host. A host without a global identity uses the Bevy entity key only within that running World and is outside cross-session replay ordering. The production `sensor` Rhai scene attaches the higher-ID reader before the lower-ID writer and verifies that the reader observes the writer's write after async preparation; it passed across Compute widths 1 and 24. | Landed and production multi-actor Rhai acceptance passed |
| D11 | Fixed | Telemetry events previously preserved observer arrival order and could hide an older event behind a newer tick in the FIFO queue. Eligible batches now use a stable typed total order before delivery. | Landed in current branch |
| D12 | Fixed | Connected USD events and serialized Modelica step requests previously followed ECS query order. Both dispatch paths now sort by stable scene/model identity, and each Modelica step payload sorts input assignments by name. | Landed in current branch |
| D13 | Fixed | Scene terminal events identified transactions by path and transition equality, so a late completion could collide with a later load of the same scene. A monotonic `SceneTransitionId` now travels through lifecycle events and async stage outcomes; coordinator completion and progress release require that exact id. The coordinator exposes the latest successful transition id as the shared scene generation and emits `SceneTransitionCommitted` only for a matching active completion. Failures and stale completions do not advance it. The authored `scene_transition_generation` Rhai contract passed through the rebuilt production `luncosim test` binary across two same-scene `RestartScene` transactions, observing generation 3 after readiness with a cumulative four-tick test horizon. | Engine lifecycle and scene-test runner acceptance passed |
| D14 | P1 | `RuntimeCycleSet` remains ordering metadata rather than an independent cadence driver ([`cycles.rs`](../../crates/lunco-core/src/cycles.rs)); owner-neutral context values are shared from `lunco-runtime-context`. The scene transaction coordinator owns the committed scene generation; scenario compile fences and execution routes now read it instead of a scripting-owned counter. Scenario and one-shot Rhai use `execution_context()`. `twin.lifecycle` receives `Twin/Lifecycle`, the mounted TwinId as generation, no elapsed clock, and explicit Start/Event/Stop phases; `policy_status()` exposes its exact context. Generic hook calls now carry a typed `HookInvocation`; nested `invoke_hook` forwards the active `WorldScope`, Rhai hooks expose it as an immutable `runtime_context` map, and native hook ABI v2 transports the same map. Scenario hooks, `physics.body_escape`, and authored `physics.initialization` decisions receive owner-supplied contexts; initialization uses the active-or-committed scene lifecycle generation, `Preparation`, and no elapsed clock, while physics escape is stamped from `SimTick` and `Time<Fixed>`. The scheduled `link.connected` seam now receives its active/committed scene route, distinguishes preparation from behavior, and supplies the latest `SimTick`/`WorldTime` without inventing an `Update` delta. Its Rhai policy rejects off-cycle calls, and installed faults reject the link with a warning. `scene.time.select` receives the exact transition id in `Twin/Lifecycle/Preparation`; the typed selection and time-state lifecycle edges reject stale ids; the focused `lunco-time` seam test proves stale apply/failure/clear events cannot mutate a same-path replacement. The production `comms_demo` gate passed eight assertions covering live owner acceptance and off-cycle rejection for both hooks. The production `test_hook_policies.rhai` REPL gate passed 34 assertions in `Application/Repl/Evaluation`. Scenario preparation idles behind the scene readiness gate and for languages with no attached programs; a missing generation is terminal only when live work needs a Twin route. USD Modelica source synthesis now receives the active-or-committed Twin generation in `Twin/Lifecycle/Preparation`; async completions must still match that Twin generation plus the USD generation or exact prepared instance plan. Both shipped synthesis policies reject off-cycle calls. GUI UI remains frame-driven, and terrain LOD only gates cover reselection at its owner cadence while sharing `Update`. The production `physics_initialization_policy` Rhai scene gate passes eleven lifecycle and policy assertions, including rejection of scenario-start calls to both synthesis hooks; scheduled readiness invocations produce no policy-fallback warning. The `battery_empty_actuator` gate publishes generated source and passes its source assertion, but its overall test fails because the `battery_drained` event was not observed. The production `lander_rcs` gate passes 30 Rhai assertions, including composed CollectionAPI/LunCoForceActuatorAPI facts and generated actuator-wrench source publication. | Partial; targeted owner-context changes are implemented, while broader owner adoption and independent UI/LOD cycles remain open |
| D15 | Fixed | The shared simulation composition previously installed camera-driven terrain LOD with physics terrain. `TerrainSurfaceVisualizationPlugin` now owns LOD, derived visual maps, overlays, and render-shadow binding; only the GUI presentation composition installs it. Headless server and scene-test hosts retain terrain queries/colliders without scheduling those systems. | Landed; package check and opt-in composition test passed |
| D16 | P2 | Terrain cover reselection uses a 30 Hz `Time<Real>` skip boundary and shared bounded background admission; the owner commits only results matching the current camera/profile signature, terrain generation, oracle identity, and operation. Offline capture bypasses the cadence but keeps cover preparation asynchronous. Equal tile-bake priorities close with stable coordinate and morph keys. Tile mesh bakes use per-terrain `AsyncComputeTaskPool` tasks; completed meshes are published in stable coordinate order under the per-frame terrain budget. | Partial; capture back-pressure and bounded publication are implemented, while shared-priority tile-bake admission and frame-time acceptance remain open |
| D17 | P1 | Telemetry sampling uses per-clock deadline heaps in an exclusive `FixedUpdate` pass ([`telemetry/src/lib.rs`](../../crates/lunco-telemetry/src/lib.rs)); each pass checks active clock lanes and reads only due channels. Sample callbacks use a bounded, budgeted `Telemetry` delivery cycle after simulation; overload drops old samples with a queryable count, and scene transitions discard outgoing-Twin backlog. Measured tick-throughput and end-to-end observer acceptance remain open. | Partial; due-channel capture and bounded fan-out are implemented, performance acceptance remains open |
| D18 | P2 | Cycle labels alone do not provide measurements. `CosimStatus` exposes bounded per-participant Modelica solver-step service time, dispatch-to-response latency, and native pending-task count at step start; `include_entities: false` returns a small aggregate profile for large authored scenes. The production `sensor` gate checks full and compact response contracts. The `multi_rover_stress_20` gate passed with 20 stepped participants and 2,600 samples; `CosimStatus` contained 3,329 USD-sourced rows, so per-entity details exceeded Rhai's aggregate string-data cap and the summary query is required in scripts. Two one-thread runs measured mean worker-step service of 262/255 μs, maximum service of 1.96/0.887 ms, maximum response latency of 156.9/19.0 ms, and maximum pending backlog 19 in both. Service averages were similar while response tails varied substantially. Response latency includes queueing, transport, and owner response handling, so it is not a pure queue-wait metric. These samples do not justify a global solver change; use Tracy to attribute the response tail and compare a concrete parallel execution candidate before changing the serialized worker. Browser queue depth and a shared cross-owner duration/queue/overload view remain unavailable. | Partial; bounded Modelica worker evidence and fleet profiling are implemented, cross-owner metrics and comparative profiling remain open |
| D19 | P1 | Bevy drains fixed schedules synchronously before the UI `Update`; the transport guard admits up to 64 fixed steps at its highest rate. Constant `Time<Fixed>` delta preserves step size but does not guarantee 60 wall-clock physics ticks/s, and catch-up delays input/UI. Capping by dropping overstep would break authoritative tick continuity. | Open; hard isolation needs one paced owner for the full causal simulation world plus nonblocking tick-stamped commands and immutable snapshots |
| D20 | P1 | One-shot Rhai commands now enter a bounded 64-request FIFO; excess submissions return a terminal queue-full error. `drain_world_scripts` evaluates one request per `Update`, and its engine uses a 100,000-operation ceiling per live-world invocation. FIFO order is unchanged, while one-shot work now yields between requests. Scenario hooks still execute serially in their owning schedules, and an individual native bridge call has no preemption point. | Partial; queued batches and VM work are bounded, while expensive native calls and safe worker evaluation of immutable inputs remain open |
| D21 | P1 | On native hosts, terrain cover preparation and tile bakes no longer wait inside `Update`, including during offline capture. Capture holds virtual time at the advanced frame while the existing terrain readiness projection is active; selection is evaluated for each logical capture frame. Completed tile meshes are retained and committed in stable coordinate order under a per-frame budget, and that backlog remains part of readiness. Web cover preparation still lacks worker transport. The shared four-job admission does not preempt running jobs and does not cover all direct Bevy-pool producers. | Partial; the native main-thread wait and unbounded completion burst are addressed, while shared-priority tile-bake admission, web worker transport, adversarial worker-order tests, and measured frame-time acceptance remain open |
| D22 | P1 | Repeated USD authoring previously cloned the document for one-op grouped mutations and rebuilt a deep copy of the live dependency-byte recipe for every command. One-op authored and transient groups now use their direct owner mutation path; USD's unchanged authored/runtime/view layers share immutable `Arc` snapshots across compound candidates. `CanonicalStage` exposes stage identity plus resolver-closure revision, so ordinary edits reuse the installed recipe and only a replaced stage or changed dependency closure snapshots bytes again. The thread-affine stage, journaling, and projection remain on the deterministic owner path. | Partial; focused mechanism tests and package checks are required, while add/delete waypoint latency, journal/projection cost, and startup responsiveness still need owned runtime measurement |
| D23 | P1 | USD file opens and confirmed discards previously read bytes asynchronously but parsed USDA in the frame-owned registry commit. They now parse into immutable `PreparedUsdSource` on the file task; the registry retains path deduplication, dirty-document preservation, reset-history policy, and owner-thread installation. Confirmed reset reuses the prepared SDF instead of parsing again. Root Twin composition and live edit/journal/projection costs are separate paths and remain synchronous where not already worker-prepared. | Partial; focused prepared-open/reset gates and package checks are required; production Summer Space School startup and waypoint latency still need isolated runtime measurement |
| D24 | P1 | Every new `SetAttribute` previously serialized the entire target layer as its inverse even though `RemoveAttribute` is the exact inverse for a newly-authored opinion. It now returns the typed removal op; existing values retain typed restoration, with source snapshots only for prior opinions lacking a representable typed restore. This removes growing-layer serialization from each waypoint-marker add while preserving the same journaled forward intent. The production Rhai route-runtime gate now checks creation, undo, and redo of a referenced marker's trigger attribute; Rust coverage is limited to the internal inverse-selection mechanism. | Partial; focused Rust and production Rhai checks are pending, as are Summer Space School add/delete latency and full runtime history profiling |

## Latest-main integration evidence

After integrating local `main` at `4bde746aa`, the clean production
`luncosim` build passed with `-j 4`. The `revolute_motor` Rhai scene passed all
6 assertions in 600 fixed ticks; the motor reached its 1.2 rad target. The
`prismatic_drive` Rhai scene passed all 5 assertions in 600 fixed ticks; the
authored drive held -1.5 m and a later port command moved it to -0.5 m. The
`physical_rover_controls` Rhai scene passed all 33 assertions in 2,220 fixed
ticks, including a terminal rejection for an unknown input port. The
`celestial_dataset_contract` Rhai scene passed all 11 assertions and confirmed
that `CelestialTime` accepts 100,000× while `WorldTime` and physics rates stay
unchanged; a request above the limit is rejected without changing the child
clock. The `solar_rover_energy_cycle` Rhai replay also passed all 49 assertions
in 3,247 fixed ticks with the 100,000× celestial rate, covering the solar
tracker and Modelica-driven energy path in the same production run. The
`sun_tracker` Rhai scene passed all 29 assertions in 1,561 fixed ticks; its
hinge angle followed the controller yaw from -2.4434 rad to -2.1416 rad. These
results verify that changed angular and linear motor targets wake sleeping
dynamic joint islands, allowing Avian to apply each target on the next solver
step. The `sun_tracker` run also exercised `squared_norm` in authored Rhai.
These are focused same-build production results; they do not establish
whole-simulation or cross-machine determinism. The separate
`multi_rover_stress_20` profile measurements above remain diagnostic evidence,
not a solver comparison.

## Migration order

### Generated Modelica lifecycle

The USD domain projector now publishes generated source and its parsed
interface before Modelica compilation. The generated-source owner links a
normal document, then lifecycle admission dispatches `CompileRequested` through
the same source-generation and worker-session fences used by authored models.
This avoids a second compile path whose result could arrive before the generated
participant had its document lifecycle state. The production
`route_lifecycle` scene gate is the runtime acceptance for this boundary.

### Pre-simulation identity and entity indexes

Lifecycle projection, stable identity assignment, and API path-index
publication now have explicit ordered `PreUpdate` cycles before `TimeSpineSet`
can release fixed simulation. Newly admitted referenced entities therefore
resolve through `find_path` on their first resumed tick. The production
`route_lifecycle` Rhai gate passed twice with the same seed: both runs recorded
74 assertions and 56 fixed ticks. Application update counts were 102 and 96,
so this confirms a stable simulation verdict and horizon, not a deterministic
wall-frame count.

Use the canonical staged plan in
[`62-deterministic-runtime-and-async-boundaries.md`](../architecture/62-deterministic-runtime-and-async-boundaries.md#10-migration-order).
The terrain visualization capability split (D15), first Rhai execution
context boundaries (D14), script dependency admission (D3), and shared native
async admission for Modelica document parsing, Rhai inline roots and asset AST
cache misses, Twin SysML analysis, and default Twin USD parse/serialization are
implemented in this branch.
File-backed Rhai sources are parsed in the asynchronous Bevy asset loader and
the exact text/AST pair is reused by scenario workers, startup tools, Twin tools,
and prelude installation.
D1, D2, D8–D9, remaining D14 owners, and D17–D18 remain open. D4 is partial. D5 remains
partial because whole-simulation acceptance and browser worker paths are not
implemented. D6's native scenario dependency path has production evidence;
browser worker transport remains open. D7 remains partial because sidecar
restore and browser worker transport remain open.
D16 is partial: native cover preparation uses shared admission, while tile-bake
admission and measured visual-host frame-time acceptance remain open.
Whole-simulation determinism,
physics throughput, and UI responsiveness must be verified independently before
claiming the complete contract.
