[package]
name = "lunco-api"
version = "0.1.0-dev"
edition = "2021"
license.workspace = true
description = "Transport-agnostic command, query, discovery, and response contracts for LunCoSim"

[dependencies]
lunco-core = { path = "../lunco-core" }
lunco-readiness = { path = "../lunco-readiness" }
bevy = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
