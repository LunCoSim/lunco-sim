[package]
name = "lunco-usd-viewport-ui"
version = "0.1.0-dev"
edition = "2021"
license.workspace = true
description = "LunCoSim's interactive USD preview viewport"

[dependencies]
# The viewport UI paints an egui panel and consumes runtime view facts. The
# sibling `lunco-usd-viewport-runtime` owns cameras, render targets, lights,
# and GPU features; this package only needs Bevy's ECS/app facade directly.
bevy = { workspace = true }
egui = { workspace = true }
lunco-api = { path = "../lunco-api" }
lunco-api-core = { path = "../lunco-api-core" }
lunco-doc = { path = "../lunco-doc" }
lunco-doc-bevy = { path = "../lunco-doc-bevy", default-features = false }
lunco-usd-document = { path = "../lunco-usd-document" }
lunco-usd-bevy-stage = { path = "../lunco-usd-bevy-stage" }
lunco-usd-viewport-core = { path = "../lunco-usd-viewport-core" }
lunco-usd-viewport-runtime = { path = "../lunco-usd-viewport-runtime" }
lunco-viewport-core = { path = "../lunco-viewport-core" }
lunco-workbench-core = { path = "../lunco-workbench-core" }
lunco-workbench-widgets = { path = "../lunco-workbench-widgets" }

[lints]
workspace = true
