[package]
name = "lunco-hooks-native"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Native dynamic loader that adapts shared-library providers to LunCoSim's typed hook registry"

[dependencies]
libloading = "0.8"
lunco-hooks = { path = "../lunco-hooks" }
lunco-hooks-plugin-api = { path = "../lunco-hooks-plugin-api" }

[lints]
workspace = true
