# lunco-modelica-docs-ui

Reusable egui renderer for Modelica HTML documentation.

The package owns HTML-to-Markdown conversion and CommonMark caches, plus
dispatch of workbench-owned URI links. Modelica document lookup and panel
selection remain in `lunco-modelica-ui`.
