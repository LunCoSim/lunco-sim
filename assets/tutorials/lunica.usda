#usda 1.0
(
    """
    The curriculum the LUNICA app offers. See `./sandbox.usda` for why this layer
    exists rather than a `hosts` attribute on each track.
    """
    metersPerUnit = 1
    subLayers = [
        @lunco://tutorials/lunica/curriculum.usda@
    ]
)
