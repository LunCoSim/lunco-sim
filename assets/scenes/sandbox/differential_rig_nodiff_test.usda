#usda 1.0
(
    defaultPrim = "DiffRigTest"
    upAxis = "Y"
    metersPerUnit = 1
)

# G5 isolated differential test — coupling OFF (control). Local stiffness=0
# opinion disables the coupling. RockerR should stay at ≈0 (no mirror).

def Xform "DiffRigTest"
{
    custom bool lunco:spawnable = false
    custom string lunco:description = "The same balancing-linkage test as the differential rig, but with the linkage switched off. Shows what the linkage does by showing what changes when it is gone."

    def Cube "Ground" (prepend apiSchemas = ["PhysicsCollisionAPI", "PhysxTerrainAPI"])
    {
        double size = 1.0
        double3 xformOp:translate = (0, -0.5, 0)
        double3 xformOp:scale = (200.0, 1.0, 200.0)
        uniform token[] xformOpOrder = ["xformOp:translate", "xformOp:scale"]
        color3f primvars:displayColor = (0.4, 0.4, 0.45)
        bool physics:collisionEnabled = true
        float physics:friction = 1.0
    }
    def DistantLight "Sun"
    {
        float inputs:intensity = 10000
        double3 xformOp:rotateXYZ = (-35.0, 40.0, 0.0)
        uniform token[] xformOpOrder = ["xformOp:rotateXYZ"]
    }
    def Xform "Avatar"
    {
        string lunco:avatar = "true"
        string lunco:cameraMode = "freeflight"
        custom double3 lunco:cameraLookAt = (0.0, 1.5, 0.0)
        double3 xformOp:translate = (6.0, 4.0, 7.0)
        uniform token[] xformOpOrder = ["xformOp:translate"]
    }
    def Xform "Rig" (
        prepend references = @../../vessels/rovers/differential_rig.usda@</DiffRig>
    )
    {
        float lunco:differential:stiffness = 0.0
        float lunco:differential:damping = 0.0
        double3 xformOp:translate = (0, 1.5, 0)
        uniform token[] xformOpOrder = ["xformOp:translate"]
    }
}
