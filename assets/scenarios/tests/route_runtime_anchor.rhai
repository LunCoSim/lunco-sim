// Empty executable route binding for the runtime-sidecar acceptance fixture.
// The fixture tests waypoint persistence independently from vehicle dynamics.

fn on_start(me, ctx) {}

fn on_tick(me, ctx) {}
